# 28. Анализ production-логов производительности фронтенда (07–17 авг 2026)

Этап 1 плана [25-frontend-ux-performance-measurement](25-frontend-ux-performance-measurement.implementation-plan.md) («Собрать логи неделю» → «Свести результаты, выбрать места второго этапа»). Только анализ данных. Рефакторинг/оптимизации — отдельный документ, следующий шаг.

Каждая операция ниже разобрана по длительности самой по себе — независимо от того, сколько раз она сработала за 10 дней. Редкая операция (единицы-десятки срабатываний в день) чинится с тем же приоритетом, что и частая: количество вызовов не определяет важность.

## TL;DR — главные находки по длительности

- **`money.snapshot_apply` (открытие `/money`): p50 = 7999 мс, p95 = 15173 мс.** Каждое открытие денег типично ждёт сервер 8–15 секунд при 16 счетах / ~4090 транзакциях.
- **`food.initial_load`/`food.screen_ready`/`food.diary_segment_load`/`food.stats_response_apply` (открытие `/food`): p50 ≈ 3000–4800 мс**, и это **одинаково** на desktop/mobile/tablet — разброс между платформами единицы процентов, не разы. Слабое мобильное железо — не причина; общий множитель одинаков для всех устройств.
- **`food.catalogue_load`: p50 ≈ 3000 мс** независимо от платформы и от того, откуда взяты данные (`cache`/`server`). Каталог не растёт (стабильно 468 записей) — похоже на фиксированную стоимость самого вызова, а не на объём данных.
- **`metrics.history_refresh`: p90 = 28785 мс, p95 = 45066 мс** (без учёта многочасовых выбросов — см. ниже) — тяжёлая операция каждый раз, когда срабатывает.
- **`metrics.cache_hydrate` (гидратация кэша на холодном старте): p95 = 11292 мс, max = 30722 мс.**
- **`app.route_ready` на `/metrics`: p95 = 6591 мс, max = 27095 мс** — переход на страницу иногда занимает до получаса ощущения зависания.
- **Многочасовые выбросы** (`metrics.history_refresh` до 4 468 009 мс = 74.5 мин, `food.initial_load` до 1 539 705 мс = 25.6 мин) — во всех случаях `renderMs ≈ elapsedMs`, то есть почти всё время ушло между «данные готовы» и отрисовкой (два `requestAnimationFrame`), а rAF не тикает в свёрнутой вкладке. Это методологический артефакт замера (фоновая/уснувшая вкладка), не реальное ожидание пользователя. Разобрано в §5.
- **Сборщик телеметрии сам по себе неэффективен**: `PerformanceMetricsService.record()` → `trimQueue()` делает `JSON.stringify(this.queue)` целиком на **каждый** вызов, чтобы сверить лимит 1 MiB очереди. Это архитектурная стоимость самого механизма записи, не зависящая от частоты конкретной операции — при высокочастотных источниках (realtime-обновления графиков) она складывается в постоянную нагрузку на главный поток. Косвенное подтверждение — 825 527 событий потеряно на клиенте за 10 дней из-за переполнения этой же очереди (`queueDroppedBeforeBatch`).
- **Долгие задачи главного потока (`app.long_task`) тяжелее всего на `/metrics`** (p95=703мс, max=9726мс), **чаще всего — на `/food`** (4386 событий, p95=287мс). `/money` заметно легче обоих направлений.
- **Инструментация не покрывает мутации** (`money.mutation`, `food.diary_mutation`, `food.catalogue_mutation`), фото/голосовой ввод (`food.photo_flow`, `food.voice_flow`) и холодный старт (`app.bootstrap`) — самые ощутимые для пользователя операции (добавить запись, сфотографировать еду, открыть приложение с нуля) вообще не измерены, хотя мутации точно происходили (см. §7).

## 1. Входные данные

| Файл | Записей | Размер | Диапазон |
| --- | --- | --- | --- |
| `frontend-performance-2026-08-07T10-58-55--2026-08-09T23-13-19.ndjson` | 1 439 795 | 954 MB | 07.08 10:38 – 09.08 23:13 |
| `frontend-performance-2026-08-09T23-13-19--2026-08-12T15-30-04.ndjson` | 1 421 148 | 954 MB | 09.08 23:13 – 12.08 15:30 |
| `frontend-performance-2026-08-12T15-30-04--2026-08-14T16-04-37.ndjson` | 1 425 717 | 954 MB | 12.08 15:30 – 14.08 16:04 |
| `frontend-performance-2026-08-14T16-04-37--2026-08-17T09-24-06.ndjson` | 1 440 509 | 954 MB | 14.08 16:04 – 17.08 09:24 |
| **Итого** | **5 727 169** | **≈3.73 GB** | **07.08 10:38 – 17.08 09:23 UTC (≈9.9 суток)** |

Битых/невалидных строк — 0. Ротация происходит по объёму (лимит 1 GB на файл, `internal/ws/performance_metrics.go`), не по дате — отсюда неровные границы файлов. Объём стабильно высокий все 10 дней (400К–830К записей/день), выраженного тренда роста/деградации во времени нет — проблемы, разобранные ниже, присутствуют с первого дня сбора, не накапливаются.

Ошибок (`outcome: "error"`) в логах — 3 штуки на 5.73М записей: `metrics.history_refresh` — 2, `sync.operation` — 1. Как индикатор проблем `outcome` практически бесполезен.

Потерь самой телеметрии на клиенте (переполнение 1 MiB очереди, `trimQueue()` выбрасывает старые записи) — 825 527 событий, зафиксированных через `queueDroppedBeforeBatch`. Реальный объём операций на клиентах был выше сохранённого примерно на 12–13%.

### Схема записи (для справки)

```typescript
interface PerformanceMetricRecord {
  eventId: string; timestampMs: number; sessionId: string;
  operation: string; elapsedMs: number; renderMs?: number;
  route: string; outcome: 'success' | 'error';
  attributes?: Record<string, string | number | boolean>;
  device: { platform: 'mobile'|'tablet'|'desktop'; mobileDevice; mobileScreen;
    viewportWidth; viewportHeight; screenWidth; screenHeight; dpr; touchPoints;
    hardwareConcurrency?; deviceMemory?; connectionType?; connectionRtt?; userAgent };
}
```

Бэкенд (`internal/ws/performance_metrics.go:245-249`) дописывает `receivedAt`, `userId`, `clientId`, и на первой записи батча — `queueDroppedBeforeBatch`, если клиент терял события из-за переполнения очереди.

### Как читать `elapsedMs` / `renderMs`

- `elapsedMs` = `performance.now()` от старта операции до её завершения. Для `measureAsync` — включает сетевое ожидание.
- `renderMs` (только у части операций, через `recordAfterPaint`) = время от готовых данных до **двух** `requestAnimationFrame` подряд — цена именно отрисовки/интерфейса.
- **Важно**: `requestAnimationFrame` не тикает в свёрнутой/неактивной вкладке. Если пользователь свернул вкладку между стартом и концом измерения, `elapsedMs`/`renderMs` включают всё время простоя — вплоть до часов. Разобрано отдельно в §5.
- Полный каталог операций и что каждая измеряет — в [25-frontend-ux-performance-measurement.implementation-plan.md](25-frontend-ux-performance-measurement.implementation-plan.md#метрики--полный-набор-верхнеуровневых-flows).

## 2. Методология анализа

Инструмент: read-only Python-скрипт (stdlib, venv `python3.12.8`, без внешних зависимостей), потоково построчно читает все 4 файла, для каждой операции строит точную гистограмму `elapsedMs`/`renderMs` (`Counter[int, int]`) — это даёт **точные**, не приближённые перцентили (метод nearest-rank), а не выборочную оценку. Разрезы: по операции, платформе, маршруту, календарному дню (UTC по `receivedAt`), и по «размерным» атрибутам (`days`, `entries`, `points`, `transactions` и т.п. — где стоимость операции потенциально зависит от объёма данных). Отдельно — top-20 самых медленных событий по ключевым операциям и top-30 глобально, для качественного разбора выбросов.

Полный проход по всем 5.73М записей: **47 секунд**, пиковая память — низкая (гистограммы, не сырые списки). Скрипт ничего не пишет на диск, только печатает JSON-отчёт в stdout.

Часть API устройства (`connectionType`, `connectionRtt`, `deviceMemory`, `app.long_task`) браузер-специфична: Network Information API и Long Tasks API поддержаны не во всех браузерах, участвовавших в сборе. Это ограничивает то, что можно сказать про сеть/long-task конкретно для сессий на непокрытых браузерах — см. §10.

## 3. Сводная таблица по всем операциям (`elapsedMs`, мс)

| Operation | n | errors | p50 | p90 | p95 | p99 | max |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| `app.interaction_delay` | 185 380 | 0 | 88 | 280 | 376 | 688 | 7 776 |
| `app.long_task` | 6 284 | 0 | 98 | 399 | 516 | 930 | 9 726 |
| `app.navigation_timing` | 9 | 0 | 0 | 0 | 0 | 0 | 0 |
| `app.route_ready` | 141 | 0 | 893 | 2 182 | 4 213 | 8 139 | 27 095 |
| `food.catalogue_load` | 378 | 0 | 2 988 | 8 041 | 10 809 | 15 597 | 26 875 |
| `food.catalogue_search` | 1 790 | 0 | 0 | 613 | 1 581 | 4 103 | 9 228 |
| `food.diary_segment_load` | 170 | 0 | 3 026 | 9 281 | 10 955 | 43 942 | 309 965 |
| `food.diary_unified_model` | 1 857 | 0 | 1 | 6 | 8 | 11 | 48 |
| `food.initial_load` | 361 | 0 | 4 784 | 10 910 | 15 652 | 238 005 | 1 539 705 |
| `food.screen_ready` | 167 | 0 | 4 720 | 11 940 | 14 362 | 43 974 | 309 965 |
| `food.stats_aggregate_month` | 4 | 0 | 1 | 2 | 2 | 2 | 2 |
| `food.stats_aggregate_week` | 6 | 0 | 3 | 11 | 11 | 11 | 11 |
| `food.stats_base_model` | 1 055 | 0 | 5 | 10 | 14 | 34 | 447 |
| `food.stats_charts_render` | 3 398 | 0 | 7 | 65 | 77 | 106 | 1 121 |
| `food.stats_clipped_model` | 3 372 | 0 | 0 | 0 | 1 | 7 | 28 |
| `food.stats_response_apply` | 380 | 0 | 4 027 | 9 692 | 13 334 | 198 851 | 1 539 700 |
| `metrics.cache_hydrate` | 35 | 0 | 1 682 | 8 990 | 11 292 | 30 722 | 30 722 |
| `metrics.cache_persist` | 11 436 | 0 | 627 | 1 282 | 2 160 | 3 273 | 438 412 |
| `metrics.chart_create` | 23 087 | 0 | 1 | 3 | 5 | 14 | 221 |
| `metrics.chart_update` | 5 385 446 | 0 | 1 | 2 | 4 | 10 | 26 449 |
| `metrics.dashboard_model` | 87 957 | 0 | 0 | 0 | 0 | 1 | 240 |
| `metrics.dashboard_ready` | 24 | 0 | 482 | 2 480 | 3 446 | 26 809 | 26 809 |
| `metrics.data_shape_change` | 137 | 0 | 149 | 184 | 226 | 463 | 479 |
| `metrics.granularity_change` | 317 | 0 | 321 | 661 | 868 | 1 341 | 3 399 |
| `metrics.history_refresh` | 194 | 2 | 2 010 | 28 785 | 45 066 | 3 622 056 | 4 468 009 |
| `metrics.layout_change` | 11 | 0 | 77 | 318 | 341 | 341 | 341 |
| `metrics.panel_change` | 170 | 0 | 68 | 579 | 709 | 1 525 | 2 293 |
| `metrics.realtime_batch` | 11 784 | 0 | 134 | 351 | 436 | 700 | 19 961 |
| `money.balance_chart_render` | 224 | 0 | 3 | 14 | 16 | 27 | 70 |
| `money.balance_model` | 52 | 0 | 6 | 54 | 72 | 265 | 265 |
| `money.currency_change` | 10 | 0 | 41 | 57 | 61 | 61 | 61 |
| `money.expense_chart_render` | 227 | 0 | 16 | 40 | 54 | 91 | 299 |
| `money.expense_model` | 52 | 0 | 1 | 2 | 3 | 6 | 6 |
| `money.income_chart_render` | 219 | 0 | 6 | 17 | 20 | 46 | 107 |
| `money.income_model` | 52 | 0 | 1 | 6 | 11 | 20 | 20 |
| `money.monthly_buckets` | 42 | 0 | 2 | 4 | 8 | 17 | 17 |
| `money.position_lots` | 26 | 0 | 1 | 6 | 8 | 8 | 8 |
| `money.range_change` | 142 | 0 | 33 | 65 | 87 | 114 | 153 |
| `money.screen_ready` | 10 | 0 | 306 | 9 883 | 10 880 | 10 880 | 10 880 |
| `money.snapshot_apply` | 16 | 0 | 7 999 | 13 893 | 15 173 | 15 173 | 15 173 |
| `money.transaction_list_model` | 46 | 0 | 39 | 99 | 147 | 553 | 553 |
| `sync.operation` | 701 | 1 | 544 | 1 556 | 2 178 | 3 925 | 27 056 |

Клиентские вычисления (модели/агрегации без сети: `*_model`, `stats_base_model`, `stats_clipped_model`, `diary_unified_model`, `monthly_buckets`, `position_lots`, `chart_create/update`, `dashboard_model`) почти везде в пределах единиц–десятков мс. Всё, что заметно медленнее сотен мс — операции с сетевым/серверным вызовом внутри (`*_load`, `*_apply`, `snapshot_apply`, `screen_ready`, `route_ready`, `history_refresh`, `cache_hydrate`). Дальше — разбор по каждому такому месту.

## 4. `/metrics` — детальный разбор

### `app.route_ready` (переход на `/metrics`)

p50=1188мс, p90=4934мс, p95=6591мс, **max=27095мс** (desktop, 8 ядер) — переход на страницу может занимать до получаса ощущения полного зависания.

### `metrics.history_refresh`

По `retainedPoints` (объём накопленных в памяти точек):

| retainedPoints | n | p50 | p90 | max |
| --- | ---: | ---: | ---: | ---: |
| 250 000–499 999 | 20 | 5 794 | 35 350 | 3 622 056 |
| 500 000–999 999 | 172 | 1 881 | 25 350 | 4 468 009 |

Максимумы (3.6М и 4.5М мс) — методологический артефакт фоновой вкладки, см. §5. Но и без них p90/p95 (25–45 сек) на объёме 250К–1М точек — уже реальная, ощутимая длительность каждый раз, когда операция срабатывает.

### `metrics.cache_hydrate` (гидратация IndexedDB-кэша на холодном старте)

n=35: p50=1682, p95=11292, **max=30722** — до 30 секунд на восстановление кэша с диска при 500К–1М точек.

### `metrics.dashboard_ready`

n=24: p50=482, p95=3446, **max=26809** — та же холодная загрузка, что и у `route_ready`/`cache_hydrate`.

### `metrics.granularity_change` / `panel_change` / `data_shape_change`

Все в пределах сотен мс (p95 226–868) — не выглядят проблемными.

### `metrics.chart_update` — архитектура записи, не длительность самой операции

Каждый отдельный вызов дёшев (p50=1мс, p95=4мс, max=26449мс на редких выбросах) — сама операция не медленная. Проблема в другом месте: `PerformanceMetricsService.record()` (`performance-metrics.service.ts:96-123`) на каждый вызов делает `trimQueue()` → `JSON.stringify(this.queue)` целиком (строка 242), чтобы проверить лимит очереди 1 MiB. При высокой частоте срабатывания эта сериализация растущей очереди происходит так же часто — постоянная фоновая нагрузка на главный поток `/metrics`, независимая от стоимости самого `chart_update`. Стоит проверить отдельно в коде на 2-м этапе.

## 5. Многочасовые выбросы — методологический артефакт, не реальное ожидание

Самые большие значения `elapsedMs` в датасете:

| elapsedMs | renderMs | Operation | День | Комментарий |
| ---: | ---: | --- | --- | --- |
| 4 468 009 (74.5 мин) | 4 467 494 | `metrics.history_refresh` | 12.08 | `renderMs≈elapsedMs` |
| 3 622 056 (60.4 мин) | 3 621 704 | `metrics.history_refresh` | 08.08 | `renderMs≈elapsedMs` |
| 2 546 499 (42.4 мин) | 2 545 283 | `metrics.history_refresh` | 16.08 | `renderMs≈elapsedMs` |
| 1 539 705 (25.6 мин) | 1 537 269 | `food.initial_load` | 11.08 | `renderMs≈elapsedMs` |
| 309 965 (5.2 мин) | 300 388 | `food.initial_load`/`screen_ready`/`diary_segment_load` (одна сессия) | 14.08 | `renderMs≈elapsedMs` |

Во всех случаях почти всё время уходит между «данные готовы» и «два кадра отрисовки» (`recordAfterPaint`, двойной `requestAnimationFrame`), а rAF не тикает в свёрнутой/неактивной вкладке. Это значит: операция стартовала, пользователь свернул вкладку или система ушла в сон, и таймер продолжал тикать до возврата — часы «ожидания» не были реальным ожиданием перед экраном.

Это не повод отбросить операцию как «нерепрезентативную» — `history_refresh` и `food.initial_load` всё равно объективно медленные операции даже без этих значений (см. §4, §6). Но при подсчёте p99/max по ним эти конкретные точки стоит понимать правильно, а не как «пользователь ждал час». На 2-м этапе стоит добавить в замер флаг `document.visibilityState` на старте/финише операции, чтобы отделять такие случаи автоматически, а не вручную по совпадению `renderMs≈elapsedMs`.

## 6. `/food` — детальный разбор

### Время до готового экрана

| Operation | p50 | p90 | p95 | max |
| --- | ---: | ---: | ---: | ---: |
| `food.initial_load` | 4 784 | 10 910 | 15 652 | 1 539 705 |
| `food.screen_ready` | 4 720 | 11 940 | 14 362 | 309 965 |
| `food.diary_segment_load` | 3 026 | 9 281 | 10 955 | 309 965 |
| `food.stats_response_apply` | 4 027 | 9 692 | 13 334 | 1 539 700 |

По платформам (без учёта многочасовых выбросов из §5):

| Operation | desktop p50 | mobile p50 | tablet p50 | desktop p95 | mobile p95 | tablet p95 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `food.initial_load` | 4 719 | 4 636 | 5 275 | 60 454 | 14 671 | 16 963 |
| `food.screen_ready` | 4 875 | 4 686 | 7 931 | 15 921 | 12 640 | 18 687 |
| `food.diary_segment_load` | 3 026 | 2 902 | 7 367 | 11 012 | 10 232 | 11 843 |
| `food.stats_response_apply` | 4 387 | 3 727 | 5 268 | 60 450 | 12 034 | 10 297 |

Разброс p50 между desktop и mobile — единицы процентов. Открытие `/food` стабильно занимает **3–5 секунд до готового экрана независимо от устройства**. Планшет (n=19, малая выборка) стабильно медленнее по p50 `diary_segment_load`/`screen_ready` — выборка мала для окончательного вывода, стоит перепроверить на 2-м этапе.

### `food.catalogue_load` — подозрение на фиксированную стоимость вызова

| Платформа | n | p50 | p90 | p95 | max |
| --- | ---: | ---: | ---: | ---: | ---: |
| desktop | 93 | 3 172 | 9 577 | 12 603 | 15 901 |
| mobile | 206 | 3 183 | 8 312 | 10 606 | 26 875 |
| tablet | 79 | 2 426 | 6 149 | 8 011 | 13 190 |

Каталог стабильно ~468 записей весь период (не растёт), источник — `cache`/`server` вперемешку, но **p50 ≈ 3 сек на всех платформах и независимо от источника**. Не похоже на «клиент не справляется с объёмом» (объём constant) — больше похоже на сетевой round-trip или неоптимальный путь получения каталога. Точная причина — за пределами данных телеметрии, нужен код `food-catalogue.service.ts`.

### Масштабирование по объёму данных — не compute-bound

| Operation | Бакет размера | n | p50 | p95 |
| --- | --- | ---: | ---: | ---: |
| `food.stats_base_model` (по `days`) | 1000–2499 | 1051 | 5 | 14 |
| `food.diary_segment_load` (по `entries`) | 25–49 | 89 | 3 022 | 11 012 |
| `food.diary_segment_load` (по `entries`) | 100–249 | 54 | 3 197 | 11 702 |

`food.stats_base_model` — клиентское вычисление над ~2100 днями данных за **5 мс** медианно — не узкое место, несмотря на многолетний массив. `food.diary_segment_load` при росте `entries` с ~30 до ~150 (5×) держит тот же p50 ≈ 3000мс — время **не растёт с объёмом сегмента**, значит доминирует не парсинг/рендер данных, а сетевой/серверный round trip самого запроса.

**Вывод по `/food`**: узкое место — не клиентские вычисления (`stats_base_model`, `diary_unified_model`, `stats_clipped_model` — все в районе единиц мс даже на тысячах дней), а последовательность сетевых запросов при `initial_load`/`diary_segment_load`/`catalogue_load`, каждый в районе 3 секунд, и, возможно, их недостаточный параллелизм при построении экрана.

## 7. `/money` — детальный разбор

| Operation | n | p50 | p90 | p95 | max |
| --- | ---: | ---: | ---: | ---: | ---: |
| `money.snapshot_apply` | 16 | 7 999 | 13 893 | 15 173 | 15 173 |
| `money.screen_ready` | 10 | 306 | 9 883 | 10 880 | 10 880 |

`money.snapshot_apply` (`source: server`, 16 счетов, ~4090 транзакций) — **медиана 8 секунд**, минимум в датасете 8024мс — то есть это типичное поведение при холодном/серверном снапшоте, не редкий выброс. `money.screen_ready` резко бимодален: p50=306 (кэш есть) против p90=9883 (кэша нет, ждём сервер) — амплитуда почти в 30×. Остальные money-операции (`monthly_buckets`, `position_lots`, `*_model`, `*_chart_render`) — все от долей мс до десятков мс, клиентские вычисления не проблема и здесь.

Замечание: `money.snapshot_apply`/`money.screen_ready` подтверждают изменение числа транзакций (4084→4093) за период — значит мутации происходили, хотя телеметрии по ним нет (см. §8).

## 8. Пробелы в инструментации

Операции из каталога [25-frontend-ux-performance-measurement.implementation-plan.md](25-frontend-ux-performance-measurement.implementation-plan.md), ни разу не встреченные в 5.73М записей за 10 дней:

| Категория | Операции |
| --- | --- |
| Холодный старт | `app.bootstrap` |
| Layout/scroll сводки | `app.layout_window`, `app.theme_change`, `food.layout_change`, `food.diary_column_layout`, `money.transaction_scroll_window` |
| **Мутации (запись)** | `money.mutation`, `food.diary_mutation`, `food.catalogue_mutation` |
| Медиа-flows | `food.photo_flow`, `food.voice_flow` |
| Прочие food-flows | `food.day_change`, `food.realtime_update`, `food.sync_reload`, `food.diary_cache_hydrate`, `food.stats_insights`, `food.stats_range_change` |
| Прочие metrics-flows | `metrics.card_expand`, `metrics.composite_change`, `metrics.crosshair_window`, `metrics.chart_recreate` |
| Прочие money-flows | `money.tab_change` |

Самое существенное — **полное отсутствие данных о мутациях**. `money.snapshot_apply` показывает изменение числа транзакций (4084→4093 за период), `food.diary_segment_load` — изменение `entries` день ото дня: значит create/update/delete реально происходили, но ни одного `money.mutation`/`food.diary_mutation` события не долетело. Это либо код измерения на пути мутаций не подключён (`measure`/`measureAsync` не обёрнут вокруг мутирующих вызовов), либо путь есть, но систематически не проходит запись. Нужно смотреть код перед тем, как делать вывод о скорости именно записи — а это ровно тот путь, который пользователь чувствует явнее всего («добавил еду — подождал»). Аналогично `food.photo_flow` — без единой записи телеметрии за 10 дней при том, что фича в коде существует (`camera-preview.ts`).

## 9. Long tasks и interaction delay

| Платформа | `long_task` n | `long_task` p95 | `interaction_delay` n | `interaction_delay` p95 |
| --- | ---: | ---: | ---: | ---: |
| desktop | 1 890 | 597 | 132 624 | 344 |
| mobile | 2 275 | 376 | 36 321 | 256 |
| tablet | 2 119 | 295 | 16 435 | 536 |

| Маршрут | `long_task` n | `long_task` p95 | `interaction_delay` n | `interaction_delay` p95 |
| --- | ---: | ---: | ---: | ---: |
| `/food` | 4 386 | 287 | 73 979 | 376 |
| `/metrics` | 1 802 | 703 | 96 138 | 408 |
| `/money` | 87 | 267 | 15 220 | 88 |

`/metrics` даёт самые тяжёлые long task (p95=703мс, max=9726мс) при относительно небольшом их числе — согласуется с §4 (фоновая нагрузка периодически блокирует поток дольше 50мс). `/food` даёт больше всего long task по количеству (4386) при более лёгком p95 (287мс) — много коротких блокировок вместо редких длинных. `/money` заметно легче обоих (p95=267/88).

## 10. Ограничения анализа

- Часть браузеров, участвовавших в сборе, не поддерживает Long Tasks API и Network Information API — для сессий на таких браузерах `app.long_task`, `connectionType`, `connectionRtt`, `deviceMemory` не приходят вообще (не «зависаний не было», а «API недоступен»). Для полной картины по главному потоку на непокрытых браузерах ориентироваться нужно на `app.interaction_delay`, который поддержан шире.
- По `/food` и `/money` абсолютное число событий за 10 дней — низкое (десятки–сотни на операцию), доверительные интервалы у перцентилей широкие; выводы по этим маршрутам менее статистически устойчивы, чем по `/metrics`.
- Мутации, фото/голосовой ввод, холодный старт — не инструментированы, соответствующие жалобы пользователя не проверяемы по текущим логам (см. §8).
- Экстремальные выбросы `elapsedMs`/`renderMs` (сотни тысяч – миллионы мс) отфильтрованы качественно (по совпадению `renderMs≈elapsedMs`), не автоматически — см. §5.

## Чеклист

- ✅ Изучить код сборщика телеметрии (фронт) и приёмника (бэк), понять формат и семантику полей.
- ✅ Спроектировать методологию анализа (точные перцентили по гистограммам, разрезы по операции/платформе/маршруту/дню/объёму данных, top-slowest выборки).
- ✅ Написать read-only скрипт извлечения статистики, прогнать по всем 4 файлам (5.73М записей, 47 сек).
- ✅ Извлечь и задокументировать все находки в этом файле, по длительности каждой операции независимо от частоты её срабатывания.
- ⭕ На основе находок §4–§9 спроектировать план рефакторинга/оптимизации (отдельный документ, следующий шаг).
- ⭕ Решить судьбу самого сборщика телеметрии: `UPLOAD_INTERVAL_MS` помечен как «Temporary diagnostic interval» в коде, план 25 предполагал полное удаление диагностического кода после разбора данных.
- ⭕ Закрыть пробелы инструментации из §8 (в первую очередь — мутации), если понадобится ещё один цикл сбора для этих путей.
